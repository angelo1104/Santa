function occurences(string,substring){
    let numberOfOccurances = 0;

    for (let i =0; i < string.length ;i+=substring.length){
        let character = string.charAt(i);

        if (ch)

        console.log(character);
    }
}

occurences("butter","jutter")